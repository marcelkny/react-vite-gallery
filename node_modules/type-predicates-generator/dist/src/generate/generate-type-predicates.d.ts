import type { ArrayCheckOption } from ".";
import type * as to from "../type-object";
export declare function isMaybeUndefined(type: to.TypeObject): boolean;
export declare function generateTypePredicates(files: {
    importPath: string;
    types: {
        typeName: string;
        type: to.TypeObject;
    }[];
}[], asserts?: boolean, defaultArrayCheckOption?: ArrayCheckOption, comment?: boolean, whitelist?: boolean): string;
