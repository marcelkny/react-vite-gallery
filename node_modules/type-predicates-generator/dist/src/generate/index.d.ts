export declare type ArrayCheckOption = "all" | "first";
declare type GenerateOption = {
    asserts: boolean;
    watch: boolean;
    defaultArrayCheckOption: ArrayCheckOption;
    comment: boolean;
    whitelist: boolean;
};
export declare function run({ tsconfigPath, fileGlobs, output, basePath, option, }: {
    tsconfigPath: string;
    fileGlobs: string[];
    output: string;
    basePath: string;
    option: GenerateOption;
}): Promise<void>;
export {};
